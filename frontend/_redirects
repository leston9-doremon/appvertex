# Netlify Redirects
# Proxy /api/* calls to the Railway backend so the frontend doesn't need hardcoded URLs.

/api/*  https://appvertex-backend-production-82d7.up.railway.app/api/:splat  200
/admin  /app-admin-panel.html  200
/*      /index.html   200
