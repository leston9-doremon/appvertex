[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
```

**4. Save both files** (`Cmd+S`)

**5. Open VSCode terminal** (`Ctrl+`` `) and run:
```
git add .
git commit -m "fix railway root directory"
git push